# Days Left Calculator 📅

This is a simple **Days Left Calculator** web application built using  
**HTML, CSS, and JavaScript**.  
It calculates how many days are left from the current date to a user-selected future date.

---

## 📌 Project Overview
- User selects a target date
- Clicks on **Find Days**
- App calculates the remaining days from today
- Displays the result with decimal precision

---

## 🛠️ Technologies Used
- **HTML** – Page structure  
- **CSS** – Basic styling and layout  
- **JavaScript** – Date calculation logic  

---

## ⚙️ How It Works
1. The selected date is converted into a JavaScript `Date` object
2. Current date is taken using `new Date()`
3. Difference is calculated in milliseconds
4. Converted into days using:
